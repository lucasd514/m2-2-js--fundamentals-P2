// Q2
// Write a function that accepts two integers and displays
// the greater one.

// Define the function

const biggerr = function(num1, num2) {
    if (num1 > num2) {
      return num1
    }
    else {
      return num2
    }
  }


  // Call the function

  console.log(biggerr(46,99))
  

